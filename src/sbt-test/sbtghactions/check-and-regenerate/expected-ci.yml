# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: ['*']
  push:
    branches: ['*']

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    name: Build and Test
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.1, 2.12.10]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Export COURSIER_CACHE_DIR
        run: |
          echo ::set-env name=COURSIER_CACHE_DIR::$(if [[ ${{ runner.os }} =~ "windows.*" ]]; then
            echo $LOCALAPPDATA/Coursier/Cache/v1
          elif [[ ${{ runner.os }} =~ "linux.*" ]]; then
            echo ~/.cache/coursier/v1
          elif [[ ${{ runner.os }} =~ "windows.*" ]]; then
            echo ~/Library/Caches/Coursier/v1
          else
            exit 1
          fi)

      - name: Cache coursier
        uses: actions/cache@v1
        with:
          path: ${{ env.COURSIER_CACHE_DIR }}
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Check that workflows are up to date
        run: sbt ++${{ matrix.scala }} githubWorkflowCheck

      - name: Build project
        run: sbt ++${{ matrix.scala }} test

      - name: Upload target directory 'target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-target
          path: target

      - name: Upload target directory 'project/target'
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target
          path: project/target

  publish:
    name: Publish Artifacts
    needs: [build]
    if: github.event_name != 'pull_request' && (contains(github.ref, 'master') || contains(github.ref, 'test'))
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.1]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Export COURSIER_CACHE_DIR
        run: |
          echo ::set-env name=COURSIER_CACHE_DIR::$(if [[ ${{ runner.os }} =~ "windows.*" ]]; then
            echo $LOCALAPPDATA/Coursier/Cache/v1
          elif [[ ${{ runner.os }} =~ "linux.*" ]]; then
            echo ~/.cache/coursier/v1
          elif [[ ${{ runner.os }} =~ "windows.*" ]]; then
            echo ~/Library/Caches/Coursier/v1
          else
            exit 1
          fi)

      - name: Cache coursier
        uses: actions/cache@v1
        with:
          path: ${{ env.COURSIER_CACHE_DIR }}
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}

      - name: Download target directory 'target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-target

      - name: Download target directory 'target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-target

      - name: Download target directory 'project/target'
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target

      - name: Publish project
        run: sbt ++${{ matrix.scala }} +publish
